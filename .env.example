PORT=3000
NODE_ENV=development

# Database - for local development without Docker
DATABASE_URL="postgresql://devuser:devpassword@localhost:5433/devdb?schema=public"

# Database - for Docker development (used inside container)
# DATABASE_URL="postgresql://devuser:devpassword@db:5432/devdb?schema=public"

# JWT Secrets (generate secure random strings for production!)
JWT_ACCESS_SECRET="your-super-secret-access-token-key-change-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-token-key-change-in-production"
